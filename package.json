{
  "name": "sistema-tcc",
  "version": "0.0.0",
  "private": true,
  "description": "Sistema de gestão de TCC (DEE) — monorepo",
  "workspaces": [
    "pacotes/*",
    "apps/*"
  ],
  "scripts": {
    "api": "npm run dev --workspace apps/api",
    "web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspaces --if-present"
  }
}
