node_modules/
dist/
build/
.env
apps/api/uploads/
# Arquivo permanente dos períodos encerrados (documentos reais; nunca versionar)
apps/api/arquivo-permanente/
*.local
.DS_Store

# Prisma — banco SQLite local de desenvolvimento (não versionar)
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
apps/api/prisma/*.sqlite

# Vite
**/node_modules/.vite/

# Logs dos servidores de dev (não versionar)
*.log
